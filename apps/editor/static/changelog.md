# Clusterfile Editor Changelog

## 1.0.3
- Add changelog page linked from version badge and navigation footer.
- Improve changelog readability with a scrollable page layout.
- Enhance release workflow with semantic version bump, tagging, and changelog draft.

## 1.0.2
- Preserve baseline key order to avoid diff-only reordering.
- Keep empty string values only when they were empty in the source.
- Coerce numeric fields by schema so VLAN stays an integer.

## 1.0.1
- UI and API version alignment.
